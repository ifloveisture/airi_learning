# 薄写与缺口

> 登记「故意不写 / 仍偏薄」项，避免与文档债混淆。事实以源码为准。

## 故意薄写

| 项 | 原因 |
|----|------|
| **移动端 Pocket 产品专题** | 进行中；仅保留 `多端应用/移动端/README` 与 CI 地图中的 workflow 名 |
| 完整复写 `AGENTS.md` | 以仓库根文件为准，工程横切只抽贡献闭环 |
| 完整复写 `apps/server/docs/ai-context/` | 云端提供导读；正文仍在 server 树 |
| VitePress 用户手册全文 | `docs/content/.../manual/` 面向终端用户配置；与 learning 读码分工不同 |

## 已补相对原缺口

Auth/OIDC、鉴权 UI、Admin、可观测导读、长期记忆边界、core-agent、云端流式 STT、Twitter、浏览器扩展、WIP 插件、Host 跟做、Spine/MMD、Bot/Agent Runbook、工程横切、a11y 底线、角色入口、**项目介绍（产品定位 + `@proj-airi`）**。

## 仍可能偏薄（知悉即可）

| 项 | 说明 |
|----|------|
| Factorio 模块 | store 级实验，见 WIP 插件索引一句 |
| 象棋完整插件源码 | 以 Host 测试 fixture 为准 |
| 本机操控非终端 grounding | 主叙事仍偏终端/macOS |
| solutions 数量 | `docs/solutions/` 篇数少，用法见工程工具 |
| PWA 专章 | Web 启动文带过，未单开 |
| 性能专章 | Devtools performance 页可探，未单开 |
| packages 组件级 API | 组文已含「怎么用」；`ui` 全表仍以 `docs/ai/context/ui-components.md` + https://airi.moeru.ai/ui 为准 |
| 独立 packages 用例外仓 | 评估后不建；见 [共享包图/演示与用例入口](./共享包图/演示与用例入口.md) |
| 包频次进 CI | 仅 `共享包图/freq-scan.mjs` + `_data/`；人工按需重跑 |
| Electron 逐事件名表 | 速查指到 `apps/stage-tamagotchi/src/shared/eventa/`；不在共享包图复抄 |
| `core-character` 实现 | 空壳；[最短调用链](./共享包图/最短调用链.md) 链 E 写今日 pipelines-audio 路径 |

有新增「故意不写」时改本表。
