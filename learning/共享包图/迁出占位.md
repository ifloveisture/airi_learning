# 迁出占位

## 定位

目录还在，**实现已迁外仓**；本仓用 catalog 依赖已发布包。你会用 = 别进错目录改代码，去消费方和 catalog 认版本。

## 查阅

| 要找 | 去 |
|------|-----|
| 任务→文件 | [任务速查](./任务速查.md) 不单列；消费方在 `stage-ui` database 相关引用 |
| 版本钉 | 根 `pnpm-workspace.yaml` → `catalog` |

## 真源

| 目录 | 现状 |
|------|------|
| `drizzle-duckdb-wasm` | README「We have moved」→ `proj-airi/duckdb-wasm` |
| `duckdb-wasm` | 同上 |

运行时包名：`@proj-airi/drizzle-duckdb-wasm`（`pnpm-workspace.yaml` → `catalog`）。

## 怎么用

1. **不要**在 `packages/drizzle-duckdb-wasm` 里找可编译源码
2. 在根 catalog 查版本；`node_modules` / lockfile 认解析结果
3. 浏览器 DuckDB 用法：跟 **`stage-ui` database** 相关代码（谁 import 了该包）
4. 上游仓库：https://github.com/proj-airi/duckdb-wasm （README 指向处）

与云端 `server-schema`（Postgres）是不同存储面。

## 演示入口

| 方式 | 说明 |
|------|------|
| 间接 | 产品/本地 Stage 使用本地库能力时 |
| 占位目录 | 无 demo |
| 外仓 | duckdb-wasm 上游 README |

## 跟读

1. 两目录 README
2. catalog 版本钉扎
3. `stage-ui` 内对 `@proj-airi/drizzle-duckdb-wasm` 的引用

## 现状

占位说明 + catalog 消费；实现外置。

## 边界

| 是 | 不是 |
|----|------|
| 迁出指针 | 本仓可编辑实现 |
| Stage 本地分析库 | 云端 PG schema |
