# 学习路径 · 通道机器人

## 目标

跟完 Discord → Channel Hub → Stage 的桥接闭环（不是独立 Agent loop）。

## 步骤（域内优先）

| # | 读什么 |
|---|--------|
| 1 | [Channel事件速查](./Channel事件速查.md) |
| 2 | [Discord竖切](./Discord竖切.md) |
| 3 | [运营Runbook](./运营Runbook.md) 实跑 |

协议包细节在跟读竖切时打开：`packages/plugin-protocol`、`packages/server-sdk`。更系统的协议课在 `learning/插件扩展/模块通道.md`（可选）。

```bash
pnpm dev:server
pnpm -F @proj-airi/discord-bot start
```

验收：Discord 消息经 Hub 进入 Stage 编排并可回帖。

Telegram/Satori 是**另一架构**，见 `learning/独立Agent/`，不要期望它们依赖同一套「仅 Channel 桥」心智。
